module.exports = {
    // `x => x` instead of `(x) => x`
    arrowParens: "avoid",
    // soft-limit of 100 chars per line
    printWidth: 100,
    // wrap long markdown lines
    proseWrap: "always",
    // indent with 4 spaces
    tabWidth: 4,
};
